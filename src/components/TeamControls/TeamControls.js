import React from "react";

function TeamControls() {
	return (
		<div className="team-controls">
			<button>Add Player to Team A</button>
			<button>Add Player to Team B</button>
		</div>
	);
}

export default TeamControls;
